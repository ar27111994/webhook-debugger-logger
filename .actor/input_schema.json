{
  "title": "Webhook Debugger & Logger",
  "description": "Powerful tool to test, debug, and log incoming webhooks in real-time.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "section_urls": {
      "title": "Endpoint Setup",
      "type": "string",
      "sectionCaption": "Webhook Generation",
      "sectionDescription": "Automatically create unique, temporary endpoints to capture incoming data.",
      "description": "Configuration for generated URLs.",
      "editor": "hidden"
    },
    "urlCount": {
      "type": "integer",
      "title": "Number of URLs",
      "description": "How many unique webhook URLs to generate for this run (1-10).",
      "minimum": 1,
      "maximum": 10,
      "default": 3
    },
    "retentionHours": {
      "type": "integer",
      "title": "Retention Period (hours)",
      "description": "How long to keep webhook URLs active before auto-deletion (1-72 hours).",
      "minimum": 1,
      "maximum": 72,
      "default": 24
    },
    "section_limits": {
      "title": "Advanced Safety & Performance",
      "type": "string",
      "sectionCaption": "Traffic Controls",
      "sectionDescription": "Define payload limits and automated parsing logic for your incoming webhooks.",
      "description": "Safety and parsing controls.",
      "editor": "hidden"
    },
    "maxPayloadSize": {
      "type": "integer",
      "title": "Max Payload Size (Bytes)",
      "description": "Maximum allowed size for the request body. Requests exceeding this will be rejected with 413.",
      "minimum": 1024,
      "maximum": 104857600,
      "default": 10485760
    },
    "enableJSONParsing": {
      "type": "boolean",
      "title": "Enable JSON Parsing",
      "description": "If enabled, the Actor will attempt to parse 'application/json' bodies into searchable objects.",
      "default": true
    }
  },
  "required": []
}
