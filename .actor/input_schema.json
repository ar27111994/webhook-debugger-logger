{
  "title": "Webhook Debugger & Logger",
  "description": "Powerful tool to test, debug, and log incoming webhooks in real-time.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "section_urls": {
      "title": "Webhook Configuration",
      "type": "string",
      "sectionCaption": "Configure your temporary webhook endpoints",
      "sectionDescription": "Decide how many URLs you need and how long they should stay active.",
      "description": "Webhook configuration section.",
      "editor": "hidden"
    },
    "urlCount": {
      "type": "integer",
      "title": "Number of URLs",
      "description": "How many unique webhook URLs to generate for this run (1-10).",
      "minimum": 1,
      "maximum": 10,
      "default": 3
    },
    "retentionHours": {
      "type": "integer",
      "title": "Retention Period (hours)",
      "description": "How long to keep webhook URLs active before auto-deletion (1-72 hours).",
      "minimum": 1,
      "maximum": 72,
      "default": 24
    },
    "section_limits": {
      "title": "Limits & Parsing",
      "type": "string",
      "sectionCaption": "Performance and Safety",
      "sectionDescription": "Control payload limits and parsing behavior.",
      "description": "Limits and parsing configuration section.",
      "editor": "hidden"
    },
    "maxPayloadSize": {
      "type": "integer",
      "title": "Max Payload Size (Bytes)",
      "description": "Maximum allowed size for the request body. Requests exceeding this will be rejected with 413.",
      "minimum": 1024,
      "maximum": 104857600,
      "default": 10485760
    },
    "enableJSONParsing": {
      "type": "boolean",
      "title": "Enable JSON Parsing",
      "description": "If enabled, the Actor will attempt to parse 'application/json' bodies into searchable objects.",
      "default": true
    }
  },
  "required": []
}
